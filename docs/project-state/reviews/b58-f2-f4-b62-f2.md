# Review ledger — B58-f2 + B58-f4 + B62-f2 (`feat/b58-perf-margin-b62-panel`)

**Date:** 2026-06-12 · **Builder:** Claude (Fable 5, main session) · **Branch:** `feat/b58-perf-margin-b62-panel`
**Changes:** B58-f2 monthly-rescore cohort-build hoist (perf, Tier-C path) · B58-f4
category-class-aware cohort label band as **model v1.1** (methodology, Tier-C) · B62-f2
WhatChangedPanel mount on the intelligence page (UI, Tier-A).
**Classification:** Tier C (scoring engine touched; highest tier wins) + UI cross-tier review for
the B62-f2 surface. Reviews run inline (load-bearing path — never deferred to the phase audit).

## Deterministic gates (pre-review)

- Backend unit: **672 passed, 1 xfailed** (local; CI is the merge gate). Frontend vitest
  **23/23** (file) / **157+** (suite pre-revision), `tsc --noEmit` clean, scoped `next lint` clean.
- Ruff clean on every touched file (206 pre-existing package-wide errors are known advisory debt).
- Secrets scan, TODO/FIXME, IGNORE-list (bearer/ES/Manrope/bare-`/v1`), advisory-verb grep on
  `git diff origin/main...HEAD`: **all none**.
- `ranking_configs_v1.json` parses; markdownlint clean on touched docs.

## Verdicts (independent agents; builder ≠ reviewer)

| Review | Agent | Verdict |
|---|---|---|
| Architect | Sonnet subagent | ACCEPT-WITH-CONDITIONS |
| Compliance | Fable subagent | ACCEPT-WITH-CONDITIONS |
| Product | Sonnet subagent | ACCEPT-WITH-CONDITIONS |
| UI | Sonnet subagent | ACCEPT-WITH-CONDITIONS |

## Conditions and disposition (revision commit `dcfac33`)

| # | Source / sev | Finding | Disposition |
|---|---|---|---|
| 1 | Architect MAJOR | UUID-string list in `MfPortfolio.id.in_()` behind `type: ignore` | **FIXED** — owner query now uses native UUID pids (`raw_pids`), no str round-trip, ignore removed |
| 2 | Architect MINOR | hoisted build omitted explicit `as_of` | **FIXED** — `as_of=today` threaded |
| 3 | Architect MINOR | no hybrid-band `compare_to_cohort` flip test | **FIXED** — both directions added |
| 4 | Compliance MAJOR (gating) | file `activated:true` ships before the v1.1 registry row exists | **ACCEPTED WITH DEPLOY CONDITIONS** — activation is the FIRST post-migration deploy action before scoring traffic; verify `registry_activated:true` via `GET /admin/scoring/v1.1/status`; divergence at end of deploy = blocker (revert file flag); registry UUID logged in session docs |
| 5 | Compliance MINOR | `approved_by` in JSON is a generic registry pointer | **ACCEPTED** — registry row UUID is backfilled into `RATING_ENGINE_CHANGELOG.md` + session docs post-activation; the DB registry stays authoritative |
| 6 | Compliance MINOR | ADR-0030 cited this ledger before it existed | **FIXED** — this file |
| 7 | Product cond-1 | confirm cohorts group by FULL AMFI leaf category | **FIXED** — `build_benchmark` docstring states leaf-equality grouping; class prefix is margin-width only |
| 8 | Product cond-2 | recalibration trigger undefined | **FIXED** — changelog: >30% single-cycle flip in any debt sub-category triggers recalibration review |
| 9 | UI cond-1 MAJOR | Card shell (12px radius + shadow) vs panel surface (14px, no shadow) geometry snap | **FIXED** — transient shell now mirrors the panel's token surface exactly |
| 10 | UI cond-2 | heading level flips h3↔h2 across fetch states | **FIXED** — h2 in every state, test-asserted |
| 11 | UI MAJOR #2/#6 | `!data` guard blanked the section pre-fetch | **FIXED** — shell renders for undefined data; test added |

## Accepted residuals (logged, not fixed)

- **Mid-run Plus-upgrade race** (Architect/Compliance): portfolio turning Plus between pre-pass and
  loop scores without category flags that month — conservative (under-flag, never escalate),
  self-heals next cycle, documented in-code.
- **`is_plus` 2 sequential selects per distinct user** in the pre-pass — acceptable at current and
  near-term scale; revisit with real Plus load (ops-gap window).
- **Liquid/overnight dead-band**: ~0.1pp spreads stay inside even the 0.5pp debt band — honest
  outcome for regulation-constrained categories; documented in the changelog.
- **Test stub category strings** (`"equity_mid"`) in the hoist-equivalence test don't exercise the
  AMFI class dispatch — the margin function is independently tested.
- **Fake-session query-order coupling** in the rescore harnesses — inherent to the harness style.
- **Duplicate-ISIN row keys** in the panel unguarded (UI NIT) — noted in `what-changed.md`.

## Methodology gate (B6/B28)

B58-f4 is a methodology change: shipped as **model_version v1.1** per the v1-activation caveat
("changing B58-f4 → a new version through this same gate"). `created_by=architecture-review`;
**`approved_by` = founder admin at deploy** via the ADR-0026 activation machinery
(`approved_by ≠ created_by` enforced). Founder authorization for implement+deploy was given
explicitly in-session (2026-06-12). ADR-0030 records the decision; the changelog carries the
v1.1 entry; the manifest↔code lockstep is test-enforced.

## Status

**Merge-eligible** once CI is green (CI is the gate, not local pytest). **Deploy-eligible** under
the user's explicit deploy instruction + standing KVM4 authority, with condition #4's activation
sequence binding. Post-deploy: backfill registry UUID (cond #5), verify labels live.
