# DhanRadar — Open Blockers & Deferred Items

Single register of deferred items. Each names the gate/phase it blocks. Clear an item only when
proven resolved (link the commit/RCA). New items get the next B-number.

## 🟢 DEPLOYED TO PRODUCTION — 2026-06-08 (live at <https://dhanradar.com>)

First KVM4 deploy is DONE. Real status of the deploy-gate blockers (detail:
`docs/ops/DEPLOY_LOG_2026-06-08.md`):

- **B48 — DONE (enforced in prod).** `ENV=production` + `DPDP_CONSENT_ENFORCED=true` on the box;
  consent enforcement VERIFIED live (signup→201, no-grant CAS upload → 403 `consent_required`).
- **B36 — DONE (first live deploy executed).** `deploy.sh`/compose brought the 8-container stack up
  on KVM4; `python -m alembic` migration chain `0001→0013` applied. `deploy.sh` fixed to
  `python -m alembic` (PR #30). Rollback not yet exercised live.
- **B37 — PARTIAL → URGENT (escalated 2026-06-11).** Backup/restore scripts present + validated;
  **NOT yet run live** — still owed: R2 India bucket + 7-yr lifecycle, nightly cron, a tested
  restore drill. **The 2026-06-11 SEV1 (prod DB destroyed by the PGDATA volume-path mismatch —
  see RCA) is exactly the loss a nightly backup would have bounded to 24h. Wire the cron NOW;
  R2 secrets are complete in the local gitignored `.env`.** Cadence per runbook: nightly backup
  (cron/systemd timer) + quarterly mandatory restore drill into a `dhanradar-drill` scratch stack.
- **B38 — PARTIAL.** App emits `/metrics` + Sentry hook; **scrape/alerts onto `etip_prometheus`
  NOT yet wired**; `SENTRY_DSN` not set on the box.
- **B34 — PARTIAL (infra/human).** R2 archival code live but bucket **India-residency NOT verified**;
  archival + backups stay best-effort/skipped until confirmed. Do not rely on the 7-yr archive yet.
- **Also open:** `ADMIN_USER_IDS` unset (admin endpoints fail-closed 404); pg_partman absent →
  auto monthly-partition rollover off (table + DEFAULT partition exist).
- **B29 NAV backfill — RESOLVED 2026-06-10 (`42c96db`, PR #74 deploy).** `nav_backfill`
  (docker run -m 2g, one-off) populated `mf.mf_nav_history` with 2,027,380 rows / 9,401 funds
  (range 2023-06-11 → 2026-06-10). Scoring now returns real labels (`on_track`/`in_form`/
  `off_track`) instead of universal `insufficient_data`. See RCA entry SEV2 NullPool for context
  on why the prior 2026-06-08 backfill was lost (migrations #67–#74 recreated the table).

## 📋 POST-LAUNCH BACKLOG — 2026-06-08 (after go-live)

- **MF wedge end-to-end FIXED** (PRs #33/#34 + dedup fix): Celery task registration, shared CAS
  volume, NAV backfill (one-off, 2.1M rows), frontend↔backend report contract, status route,
  CAS password UX, and the dedup "bounce to dead job" bug. B34 archival gated OFF (DPDP); audit
  stays in the India-resident Mumbai Postgres. CAS upload→labelled report is functional.
- **B56 `[RESOLVED — dashboard + news implemented]` 2026-06-10 — Dashboard endpoint drift fully closed.**
  New `backend/dhanradar/dashboard/` module (own router mounted in `main.py`) implements the three
  real endpoints the home screen needs: `GET /api/v1/portfolio/summary` (authed; richer rollup —
  `current_value`/`xirr_pct`/`fund_count`/`last_updated`/per-fund `funds[]` label+band; RFC7807 404
  cold-start), `GET /api/v1/indices` (reuses the Yahoo provider, Redis-cached 60s — NSE geo-blocked
  on KVM4), `GET /api/v1/instruments/top-scored?type=fund` (read-only consumer of persisted
  `mf.user_fund_scores`, user-scoped, label+band only, ranked). **`/news` now live** as
  anonymous-read headline metadata (`title/source/url/published_at/category`) via a curated
  source fallback persisted in `news.news_items` (migration `0016`) and refreshed every 30 min by
  Celery beat (`dhanradar.tasks.news.refresh_market_news`, best-effort). FE wired (extended
  `PortfolioSummary` type + summary/top-scored
  widgets render `DisclosureBundle`+NOT_ADVICE; MSW dev-only). No numeric reaches the client
  (explicit allowlist schemas; integration test asserts `unified_score` absent from the body). No
  scoring migration. News migration added: `0016_news_items.py`. Gates: 566 backend unit + ruff +
  ci_guards; FE tsc/eslint + 70 vitest; integration expanded with `/news` coverage
  tests (CI Postgres). Tier-A Builder+Architect+Compliance: **Compliance ACCEPT-WITH-CONDITIONS** —
  the top-scored label surface lacked an adjacent disclosure (non-neg #9); FIXED inline (top-scored
  now returns the disclosure envelope + the widget renders it). **Architect ACCEPT-WITH-CONDITIONS**
  — `max().isoformat()` null-guard FIXED inline; coupling/index/perf items deferred below. Ledger
  `reviews/b56-dashboard-endpoints.md`; feature doc `docs/features/dashboard.md`.
  - **B56-f1 `[OPEN LOW]`** — `DISCLAIMER_VERSION`/`DISCLOSURE_BUNDLE`/`NOT_ADVICE` live in
    `scoring/engine/schemas.py`; dashboard imports them read-only (as mood does). Move to a shared
    `compliance`/`shared` constants module so non-scoring consumers don't reach into the engine
    schema. Deferred — the move EDITS `scoring/engine/*` (a concurrent-session lane); coordinate.
    Additionally, the `_ADVISORY_RE` core verb regex (B56-f4 architect finding, 2026-06-12) is now
    duplicated in `mood/service.py` AND `news/service.py` — fold it into the same shared-constants
    move.
  - **B56-f2 `[OPEN LOW]`** — promote the reused Yahoo helpers `_quote_meta`/`_signal_value` to a
    public interface (rename) instead of importing underscore-privates from the provider.
  - **B56-f3 `[OPEN LOW]`** — add composite index `(user_id, isin, scored_at DESC)` on
    `mf.user_fund_scores` (+ migration) before the dashboard sees real load; optional: parallelize
    the 4 Yahoo index fetches with `asyncio.gather`. Latent perf only (~0 users today).
  - **B56-f4 `[RESOLVED — feat/b56-f4-news-admin-crud]` 2026-06-12** — admin news CRUD live:
    4 `RequireAdmin`-gated endpoints (`/api/v1/admin/news`), draft→publish reviewer gate (create
    always `is_active=false`), advisory-verb title screen, ingestion upserts no longer touch
    `is_active` (reviewer-gate non-clobber), admin audit on all mutations. Ledger
    `reviews/b56-f4-news-admin-crud.md`; feature doc `docs/features/news.md`.
  - **B56-f5 `[RESOLVED — 0b91826]`** — RSS ingestion live; RBI press releases + notifications
    sanctioned (ToS confirmed 2026-06-10 from rbi.org.in/Scripts/rss.aspx); `news/rss.py` has
    source registry with `enabled` flag; SEBI feed disabled (URL 404 confirmed); provenance stamped.
    Branch: `fix/b56-live-news-rss`. Closes B56-f5.
- **B57 `[P1 DEPLOYED to prod; P2 next]` — Centralised log management.** Two-tier plan in
  `docs/project-state/LOGGING_PLAN.md`. **P1 MERGED (#38) + DEPLOYED to KVM4 (2026-06-09)**:
  structlog JSON on BOTH tiers (fastapi via uvicorn-reroute + Celery), one `request_id` correlating
  HTTP→Celery→AI→`ai_recommendation_audit`, hashed `user_ref` (never raw), a test-enforced DPDP
  redaction filter (17 tests), Docker `json-file` 50m×5 per service. Tier-B Sonnet adversarial
  sign-off ACCEPT-WITH-CONDITIONS (all applied). See `ADR-0028`, `docs/features/logging.md`,
  `docs/project-state/reviews/b57-p1-logging.md`. Deploy also fixed a pre-existing worker
  OOM-crash-loop (#40/#41/#42: `--concurrency=1` + memory rebalance; RCA 2026-06-09). **Open:**
  (a) the live single-id CAS trace shows on the next real CAS upload (wiring unit-proven);
  (b) **P2 IMPLEMENTED 2026-06-09** (`feat/b41-banners-b57-p2-audit-ledger`, migration `0014`) —
  new `audit` schema + `audit.admin_actions`/`payment_events`/`security_events` (monthly RANGE
  partitions + guarded pg_partman 84mo), per-row SHA-256 tamper hash, fire-and-forget emit helpers
  wired into admin (activate disclaimer/model), Razorpay webhook, and auth (refresh-reuse + TOTP
  lockout). Tier-C ACCEPT + Tier-B Sonnet ACCEPT-WITH-CONDITIONS (PII-log + hash-isoformat fixes
  applied). See `reviews/b57-p2-audit-ledger.md`. **Merge-eligible pending CI** (migration job +
  6 integration tests); deploy = alembic `0013→0014` upgrade on KVM4 after merge. P3 = Loki;
  P4 = alerting + retention.
  Residual risks (UUID-in-message, traceback PII, base64 bytes) documented, accepted for P1.

## ⏱ PRE-DEPLOY GATE RUN — 2026-06-08 (the parked pre-deploy phase is now ACTIVE)

- **PR #28 reconciled with `main`** (merge `d07a19e`): consolidated main's PRs #22–27 (B29
  foundation, admin/ops, B6/B28, B34, B36/B37, the parallel AI commentary) with this branch's Plus
  stack. Conflict-free vs main. **CI now GREEN on the blocking checks (commit `135ad63`/`be18200`,
  deploy-readiness session 2026-06-08): backend + migrations + frontend + guards pass; `lint` stays
  advisory-red (B40 backlog).** Both merge-blockers resolved:
  - **B54 `[RESOLVED — 135ad63]` — `test_consent_writer` (was 5 fails):** root cause was a
    double-encode in `apply_consent_change` — `cast(json.dumps(payload), JSONB)` passed a STR through
    SQLAlchemy's JSONB bind type, which `json.dumps`'d it a SECOND time, storing a JSON *string*
    scalar instead of an object; the reader's `isinstance(value, dict)` was then False so every grant
    read back as NOT-granted. Fix: pass the dict (`cast(payload, JSONB)` — encode once). Proven via
    SQL-compilation diagnostic; Tier-B adversarial review (Sonnet takeover, codex n/a) **ACCEPT** (no
    fail-open, 6 vectors). Load-bearing consent/DPDP path. (2 sibling `test_market_data` fails — B29
    turned `AMFINavProvider` into a DB-backed provider but 2 unit tests still asserted canned NAV —
    fixed in the same commit; DB path covered by `test_mf_nav_scoring`.)
  - **B55 `[RESOLVED — 135ad63]` — `migrations` job:** `timescaledb-ha:pg16` CI image lacks
    `pg_partman`. Fixed by stripping `pg_partman` in the CI migrations job like `pg_cron`
    (`sed -e '/pg_cron/d' -e '/pg_partman/d'`); the only migration using it (`0006`) already guards
    behind `IF EXISTS … pg_extension` + `RAISE NOTICE`-skip, so the alembic chain stays clean.
    Production `01_init.sql` keeps the strict `CREATE EXTENSION` (fail-loud). Not a branch code defect.
  - **3× `test_notifications` `RequireTier.__call__() missing db`** — **FIXED `ee059db`** (PHASE 5M
    regression: `/test` `_pro_gate(user)` → `_pro_gate(user, db)`).
  - `lint` job red = B40-followup advisory backlog (below; `continue-on-error`, NOT a merge-blocker).
    `guards` + `frontend` green.
  - **➡ Ordered operator deploy steps: `docs/ops/LAUNCH_RUNBOOK.md`** (merge → ENV=production+consent
    → R2 residency → deploy → backups+monitoring → mTLS → smoke → GO/NO-GO).
- **Phase-7 §5 governance panel RAN** — Security/Compliance ACCEPT-WITH-CONDITIONS (no blocker),
  UI/Product ACCEPT-WITH-CONDITIONS. Ledger: `reviews/phase7-predeploy-panel.md`. The Gate-0
  batched-audit checklist item is satisfied. **Deploy still needs the operational punch-list closed
  plus separate human approval** (see SESSION_STATE "DEPLOY PUNCH-LIST").
- **New should-fix items from the panel (pre-launch, code; not merge-blocking):** B49 ratelimit
  TOCTOU (Security); B50 per-channel duplicate label-change audit rows (Compliance); B51
  ScoreRing/AllocationDonut hex→`--dr-*` tokens (UI); B52 empty-portfolio silent-`done` guard
  (Product); B53 mood embed `Cache-Control`. (B41 ui-system deprecation banner + B40-followup
  ruff/mypy-blocking already tracked below.)
- **B36/B37 now merged from main** (deploy/backup scripts + runbooks); their LIVE KVM4 runs remain
  the deploy-gate action. B29 NAV seeding, B48 consent re-enforce, admin/scoring activation, and AI
  commentary consent-path are the operational launch-blockers (punch-list).

## Build sequence — functionality-first (set 2026-06-08)

**Decision (course-correction).** Recent sessions drifted into deploy-gate hardening, governance
audits, and docs while the core wedge still does not produce a real labelled report. From now,
during the development phase we build **product functionality + a minimum unit test per slice**
only. Deploy hardening, governance/activation gates, billing go-live data, and security/scaling
residuals are **PARKED** until a dedicated pre-deploy phase. Do **not** run a pre-deploy
governance audit on an inert product.

**Per slice:** build it, add one happy-path test + one critical-guard test, move on. The only
ceremony kept during dev is the deterministic gates (tests · secrets · anti-pattern grep ·
ruff/tsc) and — because it is a non-negotiable — the inline security/compliance check **only**
where a slice touches a load-bearing path (auth · consent · billing · scoring · AI gateway).
Nothing is batched to a phase audit during dev.

**Build in this order (finish each before starting the next):**

1. **B29 — seed real MF data (THE unlock). ✅ CODE ADDRESSED 2026-06-08 (58db876).** The fetch /
   seeding / beat / CAGG were already built; the gap was NAV history → engine signals
   (`mf/signals.py` + `_load_nav_series` wiring). A seeded fund now scores `on_track` (not
   `insufficient_data`); unit + integration tests added. Remaining = run the populate on a live
   TimescaleDB (deploy-gate). **Next functional slice → item 2 (B42).**
2. **B42 — responsive AppShell. ✅ DONE.** Shipped `725e3eb`+`588a719` (hamburger drawer, desktop
   sidebar preserved); focus-trap residual closed `9fe0a99`. Acceptance #1/#2/#3 met; 13 AppShell
   tests pass. **Next functional slice → item 3 (B43).**
3. **B43 — onboarding / risk-profile UI. ✅ ADDRESSED 2026-06-08 (`a9509fb`).** `onboarding/`
   module (sole writer of `risk_profile`) + 5-Q quiz + cold-start redirect; #3 separation hardened
   with a source-level guard. B44 already done. **Next functional slice → item 5 (AI MF commentary).**
4. **B44 — DPDP consent-capture UI.** The legal door for real users; also lets B48 be re-enabled
   later. Touches consent (load-bearing → inline check stays). Min test: grant writes
   `users.dpdp_consents`; revoke clears it.
5. **AI MF commentary (first AI consumer). ✅ ADDRESSED 2026-06-08 (`2b967d7`).** `mf/commentary.py`
   (the first `OpenRouterGateway.complete()` consumer) wires the B20/B21/B22 call sites: consent
   before payload, `model_used` audit, `<0.30` floor. Best-effort in `tasks/mf.py` (a failure never
   breaks the report); no numeric float in the payload. Tier-B inline ACCEPT (Security Sonnet
   takeover + Compliance Opus); 34 tests pass. **Next functional slice → item 6 (PHASE 5M tiering).**
6. **PHASE 5M tiering (freemium + Founding Access). ✅ ADDRESSED 2026-06-08 (`af850f9`).**
   `pro_access_until`/`pro_access_reason`/`ai_taster_used_at` (migration `0011`); `deps.is_plus`
   live check + `RequireTier` OR-clause → 402; founding stamp at signup; AI commentary Plus-gated
   with a one-time Free taster (atomic claim); `create_checkout` untouched (B7/B8 inert). Tier-B
   inline ACCEPT; 431 unit pass. Go-live stays data-only (seed plans B2/B7/B8). **Next → item 7 (B35).**
7. **B35 — Mood Compass go-live. ✅ ADDRESSED 2026-06-08 (`6c192ae`).** Real macro signals via the
   adapter (MACRO_SIGNAL provider, graceful degradation), structured 200 `data_unavailable` (no more
   404), human factor labels, non-numeric `trend` (ADR-0023), and the cached `/market/mood/embed`
   HTML widget (no numeric, disclaimer baked in). Gaps e/f (event bus, pgvector history, real AI
   commentary) DEFERRED. Tier-A; 468 unit pass + 38 new. **← LAST item: functionality-first build
   sequence COMPLETE. Next phase = pre-deploy (resolve PR #28 conflicts → Phase-7 §5 governance panel).**

**Plus tracking features (post-sequence growth; justify the PHASE 5M paywall):**

- ✅ **Stored history + auto monthly re-score** — DONE 2026-06-08 (`d89f133`): append-only
  `mf_user_fund_score_history` (label+band only), `is_plus`-gated persist on upload,
  `monthly_rescore_plus_users` beat (1st/month 03:00 IST, event-consume via scoring_bridge),
  `GET /mf/history` (402 for Free). Tier-A; 477 unit pass + 9 new.
- ✅ **Multiple portfolios** — DONE 2026-06-08 (`cef5345`): `mf_portfolios` entity + `portfolio_id`
  threaded through all MF tables (data-preserving migration `0013`, existing users backfilled to a
  'Default'); portfolio CRUD; Free capped at 1 (`count>=1 && !is_plus → 402`), Plus uncapped. Tier-A;
  492 unit pass + 15 new.
- ✅ **Label-change alerts** — DONE 2026-06-08 (`ef69a28`): the monthly re-score diffs each fund's
  label vs the prior snapshot (per portfolio) and enqueues an educational `mf_label_change` alert
  that RIDES the existing deliver seam (B31 consent + quiet-hours + rate-cap + B26 audit apply; no
  parallel path). Idempotent (`inserted` guard); factual, portfolio-named, escaped, no numeric.
  Tier-B inline ACCEPT (Security Sonnet takeover + Compliance Opus); `reviews/plus-label-change-alerts.md`.
  511 unit pass + 19 new.

**→ The Plus feature set is COMPLETE** (AI commentary · history · auto re-score · multi-portfolio ·
alerts). All dev is done; the project is fully in the **pre-deploy phase**.

**PARKED until a pre-deploy phase — do NOT spend dev sessions on these:**

- Deploy / ops: B25, B34, B36, B37, B38, B40 + B40-followup, B48 re-enable.
- Governance / activation gates: B6, B11, B24, B28.
- Billing go-live (data-only seeding + tests): B2, B7, B8, B9, B14, B16.
- Security / scaling residuals (none exploitable as-shipped): B15, B17, B19, B30, B32, B33.
- Other: B41 (a 10-minute compliance banner — do opportunistically), B47 (equities phase).

The full register below is unchanged — it is the source of truth for each item's detail; this
section only sets the working order.

| ID | Item | Blocks | Source | Status |
|---|---|---|---|---|
| B1 | Auth pytest suite written but never executed locally | green CI run | `docs/features/auth.md` | ADDRESSED — CI `backend` job runs pytest (PR+push); confirm green |
| B2 | `EXACT_PLAN_TIERS` empty (substring plan→tier fallback) | billing go-live | `reviews/prebilling-hardening.md` | ADDRESSED (code, merged) — substring foot-gun removed; unmapped plan → free + error (fail-safe). Real plan_id→tier values = data-only seeding at launch |
| B3 | `RequireConsent` is a pass-through stub | any DPDP data-processing route | `docs/features/auth.md` | ADDRESSED (gate primitive) 2026-06-05 (branch `hardening/b13-b10-ci-fe`) — `RequireConsent` now fail-closed: purpose validated against the canonical taxonomy at construction; grant read FRESH from `users.dpdp_consents` (no cache → revoke honoured immediately); missing/false/anonymous → 403 `consent_required` (`deps.py`). Unit-tested. **Remaining (later Consent module):** append-only `consent_audit_log`, grant/revoke endpoints, CMP banner, erasure, the 30-min cache **with** its flush-on-revoke writer |
| B4 | `deletion_requested_at` not enforced at auth | Consent/erasure module | `docs/features/auth.md` | ADDRESSED (login denial) 2026-06-05 (branch `hardening/b13-b10-ci-fe`) — `authenticate_user` denies a new session for an account with `deletion_requested_at` set (403 `account_deletion_pending`, after password verify so it is not an enumeration oracle; `auth/service.py`). Unit-tested. **Remaining (erasure module):** at the point it SETS `deletion_requested_at`, revoke the user's refresh jtis + flush `auth:tier:{uid}` so existing sessions die |
| B5 | No CI / commit hook — deterministic gates manual | enforced gates | `.github/workflows/ci.yml` | RESOLVED 2026-06-05 — CI runs `ci_guards.py` + secret scan + pytest + FE build on PR+push (local commit hook still optional) |
| B6 | Two-person scoring methodology gate not yet enforced | **production activation** of any `ranking_configs` version (NOT implementation) | `AI_GOVERNANCE_MODEL.md` §7.2 | **MECHANISM BUILT 2026-06-07** — `assert_activatable` enforces `approved_by ≠ created_by` (reuses `governance.two_person_gate_ok`; UUID `created_by` rejected by `EngineConfig.validate()`); admin endpoint `POST /api/v1/admin/scoring/{version}/activate` (`RequireAdmin()`); `compliance.rating_engine_changelog` is the authoritative registry; `uq_engine_changelog_activated_per_version` partial-unique index (migration 0009) is the multi-worker double-activation backstop; engine sync path keeps the JSON file-flag fallback; `provisional` governed by the registry. Independent adversarial review (Sonnet takeover; codex n/a) ACCEPT-WITH-CONDITIONS — all 3 conditions applied. Ledger: `reviews/b6-b28-scoring-activation.md`. **REMAINING (data + human gate):** real §8 backtest pass-gates must clear AND a human approver admin must activate (`approved_by` UUID ≠ authoring role `"architecture-review"`). Not a code gate — no further implementation needed. **RESOLVED 2026-06-11 — v1 ACTIVATED in production**: human approver (founder) asserted the backtest pass-gates + approved in-session; registry row `e1d46e5d-f98f-4f15-938b-44135db02d3b` written via `activation.activate_model_version` (two_person_ok=true), `audit.admin_actions` row emitted; file flag flipped in the same-day PR. See `backend/dhanradar/scoring/RATING_ENGINE_CHANGELOG.md` v1-ACTIVATED entry. |
| B7 | Razorpay `plan_id` conflation (catalog id passed as Razorpay plan id) | billing go-live | `reviews/prebilling-hardening.md` | ADDRESSED (code, merged) — `billing.plans.razorpay_plan_id` added (mig 0003); checkout fail-safes (503) until set. Real plan_ids = data-only seeding at launch |
| B8 | `_TOTAL_COUNT=12` hardcoded regardless of plan interval (annual ⇒ 12 years) | non-monthly plans go-live | `reviews/prebilling-hardening.md` | ADDRESSED (code, merged) — `billing.plans.total_count` added + used; checkout fail-safes if unset. Per-plan value = data-only at launch |
| B9 | Billing test/UX gaps: no 502-gateway-down test (no-double-charge unproven); `/billing/webhook` valid-event untested; plans-seed copy needs a Compliance pass; add `Retry-After:60` on the lock-held 409 | "done" on billing; billing go-live | `reviews/stage2-steps5-7-backend.md` | ADDRESSED (code/tests) 2026-06-05 (branch `hardening/b13-b10-ci-fe`) — `Retry-After: 60` added to the lock-held 409 (`billing/service.py`; RFC7807 handler forwards `exc.headers`); 502-gateway no-double-charge test (first attempt 502 + 1 gateway call, retry 409 + Retry-After + still 1 call) and valid-event test through the re-mounted `/billing/webhook` added (`tests/integration/test_billing.py`). **Remaining (data-only):** plans-seed copy Compliance pass — no seed exists yet; gate at billing go-live like B7/B8. Tests compile+collect locally; execute in CI (no local PG, per B1) |
| B10 | Frontend hardening: ESLint import-isolation is a fail-open N×N matrix; `NEXT_PUBLIC_API_URL` can strip `/api/v1`; ScoreRing aria split-model | before new feature slices | `reviews/stage2-steps2-4-frontend.md` | RESOLVED 2026-06-05 (branch `hardening/b13-b10-ci-fe`) — matrix replaced by generic `eslint-plugin-boundaries` rule (auto-covers all features incl. previously-uncovered `dashboard`/`mf`; clean-pass + planted-violation both verified); `apiClient` fails closed if `NEXT_PUBLIC_API_URL` ≠ `…/api/v1`; `ScoreRing` single accessible name via sr-only `<figcaption>` + decorative SVG. RCA logged |
| B11 | `concentration` risk sub-factor unweighted in `ranking_configs_v1` + `FINAL_SCORING_SPEC` §2.5/§6.1 internal inconsistency | scoring v1 activation | `reviews/stage2-step8-ranking-configs.md` | RESOLVED 2026-06-05 (ADR-0020, architecture-owner decision: option C) — reconciled as documentation: §2.5 = factor catalog, §6.1 = activated subset; concentration is catalogued-but-unweighted in v1 (no contradiction), a **v2 candidate** gated to a data source + the backtest/two-person gate (B6). §2.5/§6.1 cross-pointers + `_concentration_note` updated. No v1 weight change |
| B12 | `ci_guards.py` advisory detection too narrow (snake_case only) AND never scanned the token files | reliable enforcement of non-neg #1 | `reviews/prebilling-hardening.md` | ADDRESSED (merged) — broadened to camelCase/Title/spaced/quoted/key + now scans `tokens.json` + generated token files. Residual coverage gaps → B13 |
| B13 | `ci_guards.py` advisory coverage residuals: scans only 3 hardcoded non-code files (other `.json/.yaml/.html` label assets unscanned); `_ADV_SKIP` (`not`/`guard`) can mask a true positive; `"scoring" in p.parts` skips the whole scoring dir | reliable enforcement of non-neg #1 | `reviews/prebilling-hardening.md` | RESOLVED 2026-06-05 (branch `hardening/b13-b10-ci-fe`) — advisory scan now walks all `.json/.yaml/.yml/.css/.html`+config under `frontend/`+`backend/dhanradar/` (minus `node_modules/.next/__pycache__`); `_ADV_SKIP` tightened (anchored phrases, dropped bare `not`/`guard`); scoring skip narrowed to `ranking_configs*` files; `backend/tests/unit/test_ci_guards.py` self-test added. RCA logged |
| B14 | Cross-account webhook `plan_id` provenance (low; signature-gated): once `EXACT_PLAN_TIERS` is populated, optionally validate plan_id against a known-good set | billing go-live (residual) | `reviews/prebilling-hardening.md` | OPEN (low residual) |
| B15 | `Retry-After: 60` on the lock-held checkout 409 advertises the full lock TTL, not the *remaining* TTL (a retry at second 59 still waits 60s) | billing UX polish | `reviews/hardening-b13-b10-b9-b3-b4.md` | OPEN (low; launch-tolerable — FE shows "retry in ~a minute". Fix: `redis.ttl(lock_key)` on the 409 path) |
| B16 | Billing test coverage residuals: no annual/non-monthly plan fixture; no `subscription.cancelled`/`completed` test via the re-mounted `/billing/webhook`; no `idempotency_key_conflict` response-shape test | "done" on billing; billing go-live | `reviews/hardening-b13-b10-b9-b3-b4.md` | OPEN |
| B17 | `eslint-plugin-boundaries` rule covers feature→feature but not `shared → feature` (a component/lib importing a feature internal) — layering gap | next ESLint hardening pass | `reviews/hardening-b13-b10-b9-b3-b4.md` | OPEN (low; pre-existing) |
| B18 | AI premium hard-cap is check-then-act (not atomic): concurrent Sonnet spillovers can overshoot the $9.50 cap by up to one call's cost | AI go-live at scale | `reviews/phase3-market-data-ai-gateway.md` | **RESOLVED 2026-06-06** — `budget_guard` reworked to atomic **incr-then-rollback** (`budget.py`): reserve via atomic `INCRBYFLOAT`, admit on the pre-reservation value, release-on-reject / rollback-on-failure / reconcile-to-actual on clean exit; release+reconcile are best-effort (`_adjust_quietly`) so a Redis blip never masks the caller's outcome (daily EXPIREAT self-heals). Concurrent callers now see each other's reservations → at most one admitted past the cap (irreducible single-call cost). Warns if a premium call's actual cost > reserve (`_PREMIUM_RESERVE_USD=$0.20`). 21 budget tests incl. a real `asyncio.gather` race (positive control) + Redis-rollback-failure. Independent adversarial review (Sonnet takeover; Codex unavailable) ACCEPT-WITH-CONDITIONS, all 4 conditions applied. RCA 2026-06-06 |
| B19 | Circuit breaker is in-process per-adapter-instance — N workers each trip independently (up to 5×N failed calls); not distributed | horizontal scaling | `reviews/phase3-market-data-ai-gateway.md` | OPEN (low; Redis-backed breaker before multi-worker scale) |
| B20 | AI gateway sends `messages` to OpenRouter (non-Indian) with no cross-border/`RequireConsent` gate — must be enforced at the CALL SITE before any user-specific payload reaches `complete()` | DPDP; AI go-live | `reviews/b20-ai-callsite-gate.md` | **ADDRESSED (defense-in-depth) 2026-06-07** — `complete()` is now fail-closed default-deny: keyword-only `contains_personal_data=True` + `cross_border_consent_verified=False`; a personal-data call without verified consent raises `ConsentNotVerifiedError` as the FIRST statement, before any payload reaches OpenRouter (gateway stays module-isolated). Independent adversarial review ACCEPT; 3 deny-path tests (positive controls). **RESOLVED (call site) 2026-06-08 (`2b967d7`)** — `mf/commentary.py` `generate_commentary()` calls `assert_consent(user_id, "cross_border_ai", db)` BEFORE building any payload and passes `cross_border_consent_verified=True`; deny path returns `unavailable` without touching the gateway (`test_consent_deny_never_calls_gateway` asserts `gateway.calls == 0`). Tier-B Compliance (Opus) ACCEPT. Mirrors B31 |
| B21 | `ai_recommendation_audit` (model_used + in-force disclaimer version) not written; `complete()` does not return the winning `model_used` for the caller to record | AI go-live | `reviews/b20-b21-b22-mf-ai-commentary.md` | **RESOLVED 2026-06-08 (`2b967d7`)** — `complete()` now returns `CompletionResult(output, model_used)` (`_LLMResult` threads the winning model; free pool + Sonnet spillover both carry it). `mf/commentary.py` writes `record_served_label(surface="mf_commentary", label, model=model_used, disclaimer_version=in-force, recommendation_type="educational_label")` on the served path only. 15 gateway test call-sites updated; happy-path test asserts the audited `model`. Tier-B ACCEPT |
| B22 | `confidence < 0.30 → insufficient_data/refuse` (non-neg #4) is not enforced at the AI layer — must be applied upstream before any AI commentary is published | AI commentary go-live | `reviews/phase3-market-data-ai-gateway.md` | OPEN (caller/scoring layer). **Infra ready 2026-06-07:** the `compliance.ai_low_confidence_log` table + the fire-and-forget `compliance.service.log_low_confidence(...)` helper are BUILT (migration 0008) — only the CALL SITE remains, and it rides with the first AI-consumer PR (the gateway has no consumer yet, like B20/B21). Wire `log_low_confidence` at the AI-layer sub-0.30 refuse seam when that consumer lands. **RESOLVED 2026-06-08 (`2b967d7`)** — `mf/commentary.py` checks `res.output.confidence < 0.30` → calls `log_low_confidence(surface="mf_commentary", confidence_score, confidence_band, model, reason="below_floor")` and returns `insufficient_data` WITHOUT writing the served-label audit (`test_confidence_floor_returns_insufficient_data`: log called, audit NOT). Tier-B ACCEPT |
| B23 | Advisory reject-list (`ai_gateway/quality.py`) is the CORE set, not exhaustive — must become a versioned, domain-expert-signed-off taxonomy (and kept in sync with `ci_guards.py`) | AI go-live | `reviews/phase3-market-data-ai-gateway.md` | **RESOLVED 2026-06-11 — expert sign-off recorded.** Domain-expert sign-off on the advisory reject taxonomy attested by the founder (in-session 2026-06-11). Version = git-versioned: `ai_gateway/quality.py` `_ADVISORY_TERMS` (runtime superset incl. phrase forms) + `scripts/ci_guards.py` `_ADV_WORD`/`_ADV_HARD` (core source-scan set) as of main `502ee58`; core sets verified in sync this session (runtime is a deliberate superset). Future term additions = taxonomy change → re-sign-off + keep both lists in sync. |
| B24 | Label rule-table precedence: `manager_change`/`structural_concern` veto `in_form` even over 1Y+3Y outperformance (no recency window) — may be product-incoherent | scoring v1 activation | `reviews/phase4-rating-scoring-engine.md` | OPEN (architecture-owner/spec decision; implemented as a fail-safe caution veto + documented; recency-window = methodology change → two-person gate) |
| B25 | `/internal/v1/score` numeric endpoint relies on network topology + a shared token; full internal-network policy / mTLS not in place | scoring numeric go-live | `reviews/phase4-rating-scoring-engine.md` | ADDRESSED (defense-in-depth) — fail-closed `X-Internal-Token` guard added (disabled if unset); network/mTLS policy is a deploy gate |
| B26 | `ai_recommendation_audit` linkage: every served score/label must persist `(label, model_used, disclaimer_version)` to the 7-yr audit table | any AI/score surface go-live | `reviews/b26-compliance-audit.md` | **ADDRESSED (built)** 2026-06-06 — Compliance Audit module: `compliance` schema + Alembic 0006 (partitioned 7-yr `ai_recommendation_audit` w/ DEFAULT partition + guarded pg_partman; seeded `disclaimers`); fire-and-forget `record_served_label`; **both live seams write** `(label, model, disclaimer_version)` — MF report at generation (`tasks/mf.py`) + notification at deliver (`tasks/misc.py`); served surfaces stamp the SAME version (MF `PortfolioReport` + footers). Allowlisted `recommendation_type` (DB CHECK + service). ADR-0022. Tier-B review ACCEPT-WITH-CONDITIONS. **Admin-module progress:** audit-write-failure metric DONE (B34, `84903df`); admin-auth foundation DONE 2026-06-07 (`RequireAdmin` fail-closed surface-hiding 404 gate + `settings.ADMIN_USER_IDS` config allowlist; `reviews/b26-admin-auth.md`); **admin ENDPOINTS DONE 2026-06-07** — admin router (`dhanradar/admin/`, `RequireAdmin()`-gated): `POST /admin/disclaimers`, `POST /admin/disclaimers/{version}/activate` (single-active transition + R2 HTML snapshot + cache flush; concurrent loser → 409 via the `uq_disclaimer_active_per_type` partial-unique index), `GET /admin/audit/label-churn` (reuses `governance.review_batch`, >5% human-review gate). Alembic 0008: `rating_engine_changelog` + `ai_low_confidence_log` tables (no writer yet) + the partial-unique index. Tier-B Security ACCEPT-WITH-CONDITIONS (Sonnet takeover; codex n/a) — content-length bound + churn-type allowlist + atomic single-active index, all applied in-session. 10 unit + 10 integration tests; `reviews/b26-admin-endpoints.md`. **Remaining (Admin module):** the `rating_engine_changelog` WRITER (B6/B28 scoring-activation gate, next slice); `ai_low_confidence_log` WRITER (B22 confidence-floor consumer) |
| B27 | Signal-name taxonomy: `LabelSignals.contributing/contradicting` are free-text — needs a canonical signal-name set for consistent UI across instruments | MF report UI consistency | `reviews/phase4-rating-scoring-engine.md` | **RESOLVED 2026-06-13** — fix/b27-canonical-signal-names; new `scoring/engine/signal_names.py` canonical registry; 9 approved phrases now single-sourced + byte-pinned; public API unchanged; Tier-C ACCEPT (ledger: `reviews/b27-canonical-signal-names.md`); merged PR #115; deploy human-gated |
| B28 | Engine activation: weights are PROPOSED v1 (`activated:false`) — results are now tagged `provisional_model`, but full activation (backtest pass-gates + calibration + two-person gate) must clear before any numeric is treated as authoritative | scoring v1 production activation | `reviews/phase4-rating-scoring-engine.md` | **MECHANISM BUILT 2026-06-07** — same mechanism as B6: admin endpoint `POST /api/v1/admin/scoring/{version}/activate`; `backtest_passed` flag enforced by `assert_activatable` (422 `backtest_not_passed` if false); `compliance.rating_engine_changelog` registry row is the authoritative activation state; `uq_engine_changelog_activated_per_version` partial-unique index (migration 0009) guards against multi-worker double-activation; engine sync path unchanged (JSON file-flag fallback); `provisional` = NOT `registry_activated`. Independent adversarial review ACCEPT-WITH-CONDITIONS (all 3 conditions applied). Ledger: `reviews/b6-b28-scoring-activation.md`. **REMAINING (data + human gate):** real §8 backtest pass-gates (human/data) must clear, then a human approver admin activates via the endpoint. v1 stays provisional until then. Not a code gate. **RESOLVED 2026-06-11 — v1 ACTIVATED** (same activation record as B6: registry row `e1d46e5d`, two_person_ok=true, backtest asserted by the human approver; file flag flipped so results drop `provisional_model`). Accepted-at-activation caveats: B24 fail-safe veto + B58-f4 debt-margin calibration stay open as future-version methodology items. |
| B29 | MF data pipeline. **Code ADDRESSED 2026-06-08 (58db876):** the AMFI NAV daily fetch (`nav_daily_fetch`, real `NAVAll.txt`), `mf_funds` scheme-metadata seeding, beat schedule (23:30 IST), and the `mf_nav_monthly_agg` CAGG (migration 0008a) were already built; the missing seam was **NAV history → engine signals** — the CAS pipeline scored `FundSignals(isin=...)` (empty axes) → `insufficient_data` for every fund regardless of data. New `mf/signals.py` (`compute_fund_signals`) derives momentum + risk axes from a fund's own NAV series; `tasks/mf._load_nav_series` reads `mf_nav_history` and `_run_pipeline` now scores on real signals + values the snapshot on the latest NAV. **Proof:** a seeded 14-month series scores `verb_label=on_track`, `band=medium` (not `insufficient_data`). Unit + integration tests added. **REMAINING (deploy-gate, not code):** a live TimescaleDB must run `nav_daily_fetch`/`nav_backfill` to populate real NAV (integration test + CAGG exercise on a Timescale box). | MF report quality / go-live | `reviews/phase5-mf-module.md` | **CODE ADDRESSED (58db876); live-data populate = deploy-gate** |
| B30 | MF upload hardening residuals: `Idempotency-Key` header not handled (SHA-256 dedup covers re-upload, non-neg #6 header owed); transport-layer body-size cap (rate-limit fires after body consumed; bounded read added at app layer); consent-on-read gap (status/report not re-gated after consent revoke — documented intentional, data already processed) | MF go-live | `reviews/phase5-mf-module.md` | OPEN (low) |
| B31 | Notification cross-border DPDP consent gate: the deliver seam sends user PII (`telegram_chat_id`/email) + labels + scheme names to NON-Indian processors (Telegram, Resend/Tokyo) with only a `channels_enabled` opt-in — NOT a DPDP cross-border consent grant. Needs a `RequireConsent`-equivalent purpose gate at publish/deliver (analogous to B20), plus the `marketing`/`behavioral_nudges` purpose for promotional templates | Notification channels go-live (DPDP) | `reviews/b31-notify-cross-border-gate.md` | **RESOLVED 2026-06-06** (gate) — `tasks/misc.py` `_handle_job` **step 1b** enforces per-processor `cross_border_notify` consent (ADR-0024) fail-closed before BOTH `deliver_telegram` and `deliver_email`: no grant ⇒ `log_delivery(..., "cross_border_consent_required")` + drop (no retry). Fresh read → revoke honoured; audit logs an opaque code only. Test `test_drain_skips_without_cross_border_consent` (transport never invoked). Independent Compliance + Security review ACCEPT. **Remaining:** the `marketing`/`behavioral_nudges` grant for promotional templates + the Consent-module grant/revoke WRITER (until it lands every user fails closed = inert-but-safe). Mirrors B20 |
| B32 | Notification delivery residuals: rate-cap check-then-incr is non-atomic (safe under the single beat worker; needs Redis Lua/pipeline before multi-worker — cf. B18); `/notifications/test` rate-limit is IP-keyed (house pattern) not user-keyed; quiet-hours re-enqueue can bury a deferred job under sustained single-channel volume (FIFO migrates it tailward, no loss/dup) | multi-worker scale; UX polish | `reviews/phase6-notification.md` | OPEN (low) |
| B33 | Auth/session security hygiene residuals (Phase-7 §5 adversarial findings, none exploitable as-shipped): (a) login/signup rate-limit is GET-then-INCR (TOCTOU; bounded first-burst slip — make atomic like `budget._init_key_if_missing`); (b) the tier cache `auth:tier:{uid}` is NOT flushed at the point erasure SETS `deletion_requested_at` (the access denylist independently blocks reuse; flush owed in the erasure/Consent module); (c) `logout_user` relies on the caller (router) also calling `revoke_access_jti` — fold into one `full_logout` or assert; (d) `OpenRouterGateway.complete()` accepts caller `messages` with no `system`-role guard (safe while only Admin templates call it; add a guard before any user-text interpolation); (e) JWT decode could pin `options={"verify_signature": True}` explicitly (alg whitelist already blocks alg:none/HS confusion); (f) premium budget `INCRBYFLOAT` sub-cent drift (immaterial at $9.50); (g) the new auth entry points `/auth/totp/login` + `/auth/google/{start,callback}` (PR #99, ADR-0029) reuse the same `RateLimit` class and inherit (a)'s GET-then-INCR TOCTOU — fix once, centrally. | hardening polish; erasure/Consent + multi-worker | `PHASE7_VERIFICATION.md` | OPEN (low) |
| B34 | Compliance-audit R2 archival cross-border PII: `archive_audit_daily` exports `user_id` (DPDP personal data) to R2 as gzip-JSONL. The R2 bucket must be verified **India-resident** before archival is enabled in production. Do NOT pseudonymize the archive (it is the 7-yr SEBI record-of-serving and must stay user-identifiable per ADR-0022) — the control is bucket residency. Also owed: a reconcile job asserting every audited `disclaimer_version` exists in `disclaimers`; an audit-write-failure metric (systemic-outage alerting) | Compliance archival go-live (DPDP) | `reviews/b26-compliance-audit.md` | **PARTIALLY ADDRESSED 2026-06-07** — the two CODEABLE items are DONE: (1) audit-write-failure metric (`bump_audit_metric` → `metrics:compliance:audit_write_failures:{day}`, on the fire-and-forget failure path) and (2) `reconcile_audit_disclaimers` beat task (02:30 IST) flagging audited `disclaimer_version`s missing from the registry. **Still OPEN (deploy gate, human/infra):** R2 bucket **India-residency** verification before archival is enabled — do NOT pseudonymize (7-yr SEBI record-of-serving per ADR-0022); the control is residency. Archival stays best-effort + R2-unconfigured in dev. **Verified state 2026-06-12:** `get-bucket-location` returned an APAC location hint. Cloudflare R2 currently offers no India jurisdiction option (available jurisdictions: EU and FedRAMP only) — the `"jurisdiction":"in"` requirement is not satisfiable as-written today. **Operator/counsel decision required** before compliance-archival or backups of user data can be declared residency-compliant. Tracked in `docs/ops/backup-restore-runbook.md` (Purpose section). |
| B35 | Mood Compass go-live gaps (the module is built + governed but the public feature is inert/partial): (a) the Market Data Adapter providers are stubbed → all 11 inputs missing → snapshot skipped → `GET /market/mood` 404s (needs real signals, B29-class); (b) `GET /market/mood/embed` creator widget not built (the primary distribution surface); (c) prefer a structured 200 `data_unavailable` over 404 for the anon magnet; (d) factor evidence lists carry raw keys (`put_call_ratio`) — need human display labels; (e) the structured `mood.snapshot.published {snapshot_date,mood_score,regime,confidence_score}` event for the future AI-Enrichment analogue consumer (emission is currently audit + public card only, no event bus); (f) `mood_history` (pgvector) + real AI commentary (Admin/AI-Enrichment prompts) deferred; (g) a non-numeric directional `trend` field (ADR-0023) as the shareable hook | Mood Compass public go-live | `reviews/mood-compass.md` | **ADDRESSED 2026-06-08 (`6c192ae`)** — a,c,d,g,b done: real macro signals via a `MACRO_SIGNAL` adapter provider (best-effort NSE, graceful degradation); `GET /market/mood` returns structured **200 `data_unavailable`** (no 404); factor labels humanized; non-numeric `trend`; cached `GET /market/mood/embed` HTML widget (no numeric, disclosure + NOT_ADVICE baked in). **DEFERRED (e/f):** the `mood.snapshot.published` event bus + `mood_history` pgvector + real AI commentary (Admin/AI-Enrichment) — a later load-bearing AI-consumer slice. Tier-A; 38 new tests. The live NSE provider is best-effort, NOT deploy-hardened (data_unavailable covers unreachable) |
| B36 | No deploy automation or rollback — deploy is undocumented manual SSH; no script, no runbook, no tested `alembic upgrade head` / downgrade sequence | KVM4 deploy | `reviews/independent-audit-2026-06-06.md` | ADDRESSED (`7035400`, `71a3ed2`) — `scripts/deploy.sh` + `scripts/rollback.sh` + `docs/ops/deploy-runbook.md`; fixed a duplicate-0008 alembic branch that broke `alembic upgrade head` (0008→0008a, single head 0009). Residual: live up/down run + first real deploy are infra `[infra runs]` |
| B37 | No DB backup / PITR / WAL archival; Redis AOF has no backup — a KVM4 disk loss destroys all user data + the 7-yr SEBI `ai_recommendation_audit` trail | KVM4 deploy | `reviews/independent-audit-2026-06-06.md` | **RESOLVED 2026-06-12** — cron installed and verified on KVM4 host (21:30 UTC daily, flock guard, logging to `/var/log/dhanradar-backup.log`); fresh live backup verified (stamp `20260612171924`, db.dump 43.6 MB, upload SUCCESS, `backups/` prefix); **first restore drill ever: PASS** (2026-06-12 — restored alembic `0018`, auth_users 6, audit_rows 41, nav_rows 5,954,403; total 28 s). RPO ≤ 24 h; stated prod RTO ≤ 15 min (documented in runbook). Two bugs fixed during drill implementation: (1) MANIFEST `alembic_rev` always `unavailable` — bare `alembic` console script lacks CWD on sys.path; fixed to `python -m alembic` (`backup.sh:147`); (2) `restore.sh` lacked `timescaledb_pre_restore`/`post_restore` wrappers — latent hypertable restore failure; fixed (`restore.sh:210–229`). Drill records: `docs/ops/restore-drill-log.md`. **Residuals:** (i) R2 lifecycle rule = operator dashboard action — S3 Object R/W token gets `AccessDenied` on lifecycle API; scope rule to `backups/` prefix only; no rule = indefinite retention which satisfies the ≥ 7-yr floor (cost hygiene, not compliance gap). (ii) **B37-f1 LOW** — no backup-failure alerting (cron log only); e.g. Grafana alert on backup age or dead-man ping; decide at B38-residuals time. (iii) Quarterly drill cadence; next due 2026-09. WAL/PITR remains planned next tier. |
| B38 | No monitoring/metrics/alerting; `sentry-sdk` is installed but `sentry_sdk.init` is never called (error reporting inert); no `/metrics` endpoint | KVM4 deploy | `reviews/independent-audit-2026-06-06.md` | **SCRAPE WIRED + VERIFIED 2026-06-11** (PR #93 `adf73de`, deployed `27685cb`) — app-side code pre-existed (`efc6556`, 2026-06-07): `PrometheusMiddleware` + `/metrics` endpoint + `init_sentry()` DPDP-safe scrubber. Infra wired this session: dedicated `dhanradar_metrics` bridge added to `docker-compose.yml` (fastapi only; redis excluded — it is password-less on the app network); scrape job `dhanradar` (target `dhanradar-fastapi:8000`, path `/metrics`, 15 s, labels `env=production app=dhanradar`) appended to `prometheus.yml` with backup + `promtool` validation + lifecycle reload; verified `up{job="dhanradar"}=1` (25 targets, +1 vs prior 24; no etip targets affected). Public surface unchanged (`https://dhanradar.com/metrics` → 404, `/api/v1/health` → 200). Ledger `reviews/b38-metrics-network.md`; Tier-B Sonnet adversarial ACCEPT-WITH-CONDITIONS (conditions closed in-session); Compliance ACCEPT. Feature doc `docs/features/observability.md`. **SENTRY LIVE 2026-06-11 (evening):** DSN synced to `/opt/dhanradar/.env`, fastapi recreated, `init_sentry()=True`, test event delivered; the `.env` append cascaded a full-stack compose recreate (env_file hash) incl. postgres/redis — data verified intact (alembic `0017`, 3 users, 5.94 M NAV rows, 226 redis keys; the `445a487` volume fix held). **Remaining (operator action):** Grafana alert rules staged at `infra/observability/grafana-alerts-dhanradar.yaml` — operator must copy UID-filled copy from `/tmp/dhanradar-alerts.yaml` on the box into `/opt/intelwatch/docker/grafana/provisioning/alerting/` + restart `etip_grafana`. **Open residuals:** (a) any container on `dhanradar_metrics` reaches fastapi's full `:8000` surface, not just `/metrics` — future hardening: non-Authorization scrape token dual-control; (b) `etip_prometheus` network attach is imperative — lost if that container is recreated (`docker network connect dhanradar_metrics etip_prometheus` must be re-run); (c) runbook audit-write-failure + AI-budget alerts not wirable from `/metrics` (counters live in Redis — need an exporter or `/metrics` gauges); (d) Celery workers never call `init_sentry()` (only `main.py` does) — worker errors will not reach Sentry. |
| B39 | Frontend has zero tests; `vitest run --passWithNoTests` makes the FE CI job a vacuous green | FE reliability | `reviews/independent-audit-2026-06-06.md` | ADDRESSED (`a152b2b`) — vitest wired (jsdom + MSW node server), 17 tests across Input/auth-api/handlers, `--passWithNoTests` dropped, vitest global types added so tsc/next build pass. Verified 17/17 green locally |
| B40 | CI gaps: ruff/mypy never invoked; the Postgres service is `postgres:16` not TimescaleDB (extensions + `01_init.sql` untested); Alembic migrations never run in CI (`create_all` bypasses them) | reliable gates; deploy | `reviews/independent-audit-2026-06-06.md` | ADDRESSED (`ddc3f98`) — backend service → `timescaledb-ha:pg16`; NEW migrations job runs `01_init.sql` (minus pg_cron) + alembic up→down→up on the real image; ruff+mypy invoked. **Caveat (B40-followup):** ruff/mypy are ADVISORY (`continue-on-error`) — repo has ~361 ruff findings (351 import-sort autofixes) + never mypy-checked; promote to blocking after a dedicated lint-cleanup. Migrations job is self-validated on its first CI run (no GH services locally) |
| B41 | `docs/ui-system/contracts/*` (`score-model.md`, `openapi.yaml`, `schema.sql`) define SEBI-banned advisory verbs, bearer auth, public numeric score + `fair_value`, and Elasticsearch — with NO in-file deprecation warning (only `CLAUDE.md` flags the tree as harvest-not-adopt) | compliance misbuild risk | `reviews/independent-audit-2026-06-06.md` | **RESOLVED 2026-06-09** (`feat/b41-banners-b57-p2-audit-ledger`) — visible "⛔ DO NOT ADOPT — HARVEST-NOT-ADOPT REFERENCE ONLY (B41)" banner prepended to all 6 contract text files (3 flagged + analytics-events/error-catalog/route-map), each naming the specific violations + authority/MIGRATION_STRATEGY pointer. `seed-data.json` skipped (JSON, PII-free data, no banned content). Zero new markdownlint errors (pre-existing tree backlog untouched) |
| B42 | Frontend `AppShell` is desktop-only (fixed 224px sidebar, no hamburger/responsive/bottom-nav) — unusable on mobile in a mobile-first market | usable launch | `reviews/independent-audit-2026-06-06.md` | **ADDRESSED (`725e3eb` + `588a719`)** 2026-06-08 — shared `SidebarContent`; desktop `<aside>` now `hidden md:flex`; topbar hamburger opens the same nav as a `role=dialog` slide-in drawer (backdrop/Escape/nav-click/route-change close; dynamic `aria-expanded`). Folded in: `Field` `aria-describedby`+`aria-invalid` wiring; `MoodGauge` hex→`var(--dr-*)` tokens. Independent UI review ACCEPT-WITH-CONDITIONS, high+med a11y conditions fixed; 38 FE vitest pass. **Focus-trap residual now CLOSED (`9fe0a99`)** — drawer traps Tab/Shift+Tab + restores focus to the trigger on close (WCAG 2.1.2/2.4.3); 13 AppShell tests (41 FE suite) pass. Remaining low finding: Settings outside Primary nav landmark [pre-existing, non-blocking]. Acceptance #1/#2/#3 all met. |
| B43 | Onboarding / risk-profile UI. **ADDRESSED 2026-06-08 (`a9509fb`).** New `onboarding/` backend module: `POST /api/v1/onboarding/risk-quiz` (authed, RFC7807, Idempotency-Key) computes a profile from a 5-Q quiz and is the SOLE writer of `auth.users.risk_profile` (column already existed; no migration). Frontend: 5-Q accessible quiz at `/onboarding` + `AuthGuard` cold-start redirect (null `risk_profile` → `/onboarding`, loop-guarded). Non-neg #3 hardened with a new source-level guard (`test_scoring_separation.py` — tokenizes `dhanradar/scoring/`, asserts no `risk_profile` NAME token + onboarding never imports scoring). Gates: 51 backend unit + ruff + route reg + ci_guards green; 52 FE vitest + tsc/eslint/tokens green; integration writer test collects (runs on live DB). **Deferred (not needed for the cold-start slice):** the architecture's `onboarding_states` + `risk_profile_log` tables + the full 5-step funnel (steps 1/2/3/5) — a later increment. | MF product value; launch | `reviews/independent-audit-2026-06-06.md` | **ADDRESSED (`a9509fb`)** |
| B44 | No DPDP consent UI — the `RequireConsent` gate primitive (B3) is fail-closed server-side, but there is no banner/screen/prompt to CAPTURE a grant, so consent-gated routes cannot legally go live | any DPDP data-processing route go-live | `reviews/independent-audit-2026-06-06.md` | **ADDRESSED (`927f64f` backend, `4b40f83` frontend)** 2026-06-08 — the consent WRITER now exists: `POST /api/v1/consent/grant`+`/revoke`, `GET /consent` (authed, anonymous→401-first, RFC7807, action-scoped `Idempotency-Key`, per-purpose atomic `jsonb_set`, single-commit append to new append-only `consent.consent_audit_log`, migration 0010). Revoke writes `{"granted":false}` — never a `revoked` key (deps.py revoke contract). Frontend: point-of-use `ConsentModal` gating MF upload on `mf_analytics` + `settings/privacy` panel (all 7 purposes). Tier-B adversarial sign-off: codex n/a → Sonnet takeover ACCEPT-WITH-CONDITIONS, all 3 applied (0-row UPDATE guard, ORM CheckConstraint, Redis graceful-degrade). 34 unit + 14 integration + 6 FE tests. Ledger `reviews/b44-consent-writer.md`; feature doc `docs/features/consent.md`; RCA 2026-06-08. **Pairs with B48** — re-enforce the kill-switch at launch so a real grant is required. |
| B45 | Frontend runs entirely on MSW mocks (`providers.tsx` auto-enables in dev); no flow has ever been exercised against the real backend — integration is unproven | launch | `reviews/independent-audit-2026-06-06.md` | ADDRESSED (`a152b2b`, `ddc3f98`) — CI build now runs `NEXT_PUBLIC_API_MOCKING=disabled` (mocks-off, proves no mock dependency); Playwright `e2e/smoke.spec.ts` smoke test against `SMOKE_BASE_URL` asserts no MSW banner + no page errors. Residual `[infra]`: wire the smoke test into a real backend+frontend staging run (not run in CI until a deployment exists) |
| B46 | CAS job failure renders an infinite spinner — the report page only checks `status === 'done'`; a backend `status: 'error'` never surfaces to the user | MF UX | `reviews/independent-audit-2026-06-06.md` | ADDRESSED (`c86c413`) — `useCasStatus` stops polling on `done`/`error`; report page renders an error card + "Upload again" CTA on `status:'error'` |
| B48 | DPDP consent gate temporarily DISABLED for pre-launch dev (`DPDP_CONSENT_ENFORCED=false` in dev `.env`) so consent-gated routes work before the consent-capture UI (B44) exists | **launch deploy** (the gate MUST be re-enforced before any real user data) | user decision 2026-06-07; `reviews/b48-consent-killswitch.md` | **OPEN (must re-enable before launch)** — env kill-switch at the single `_consent_granted` chokepoint (disables `RequireConsent`/`consent_granted`/`assert_consent`). Fail-safe by design: DEFAULT `true` (enforced); the bypass takes effect ONLY in an allowlisted `development/test/ci` ENV (`settings.consent_bypassed`); setting it `false` in any other ENV is a **hard boot failure** (`config.model_post_init`), so it cannot reach production/staging; one startup warning when active. Independent Security review (Sonnet takeover; codex n/a) ACCEPT-WITH-CONDITIONS — env-allowlist invert + boot guard both applied. Tests: bypass-on grants all (dev), ignored in prod, boot-guard rejects 6 non-dev envs, default-enforced. **Re-enable action at launch:** set `DPDP_CONSENT_ENFORCED=true` (or delete the line) in the deploy env AND set `ENV=production` (which alone forces enforcement) — then verify gated routes 403 without a grant. **Production-enforcement PROOF added 2026-06-08 (`135ad63`/`be18200`):** `tests/unit/test_b48_consent_prod_guard.py` (6 tests — prod/staging/unknown-env + flag-off → hard boot crash; prod+flag-on → `consent_bypassed False`; dev may bypass) + `test_consent_writer.py::test_consent_gated_route_refuses_without_grant` (un-granted user → 403 `consent_required` RFC7807). Code+test complete; only the operator env-set remains (`docs/ops/LAUNCH_RUNBOOK.md` Step 2). Pairs with **B44** (consent-capture UI must ship so users CAN grant) and **B20/B31** (cross-border grants) |
| B47 | Kite Connect (Zerodha) equities/ETF provider not implemented — `KITE_API_KEY`/`KITE_API_SECRET` parked in `.env` (+ `.env.example`), but the Market Data Adapter `kite` rung is still a stub. Needs: real provider against the adapter contract; a daily access-token refresh (token is runtime via login-redirect exchange, not in `.env`); market-data ONLY, never order placement (non-neg #1); and a data-redistribution ToS clearance before any Kite quote reaches a non-Zerodha client (Compliance / likely ADR) | stocks/ETF phase (NOT the MF-first launch) | `.env.example`; architecture data-source ladder (`DhanRadar_Architecture_Final.md` Rec 5) | OPEN (LOW) — key stored; wiring deferred to the equities phase |
| B58 | **Scoring labels are degenerate — the core differentiator produces no signal.** The category-relative label inputs (`outperform_1y`, `outperform_3y`, `underperform_12m`) are hardcoded `False` because no cohort-benchmark query exists; the NAV-only path feeds the rule table only own-series momentum/risk. Result: the label table (`scoring/engine/labels.py:22-41`) can only ever emit `on_track` (data present) or `insufficient_data` (no NAV) — never `in_form`/`off_track`/`out_of_form`. Every fund a user uploads is therefore labelled identically. The "explainable, differentiated labels" value prop is currently inert. Found by the 2026-06-09 progress audit. | MVP (core product value); MF wedge quality | `scoring/engine/signals.py:26-27`; `PROGRESS_AUDIT_2026-06-09.md` | **RESOLVED 2026-06-09** (`fix/b58-cohort-labels`) — peer-cohort benchmark built from existing AMFI NAV data (no new source, **no migration**): new pure `mf/cohort.py` (per-category MEDIAN of peers' 1Y/3Y return + max-drawdown; beat/trail by > 2.0pp margin → in_form/off_track; `on_track` inside band), `mf/signals.long_horizon_stats` + `compute_fund_signals(category_relative=…)`, wired via `tasks/mf.py:_compute_cohort` at both scoring sites. `out_of_form` stays honestly unreachable (needs a `structural_concern` fundamentals signal not yet ingested). Gates: 557 unit pass + ruff + ci_guards. Tier-C panel ACCEPT-WITH-CONDITIONS ×3 (`reviews/b58-cohort-labels.md`): cond-fixes landed inline (escalation-free copy, thin-cohort + short-track explainability notes, stats-cache, var rename); follow-ups **B58-f1..f5** below. Thresholds `provisional_model` pending B6/B28 activation. **Merge-eligible; deploy needs Phase-7 §5 log + human approval.** |
| B58-f1 | Pin the 5 approved educational label phrasings (`ahead of/behind category peers`, …) into the anti-pattern grep corpus so a future mutation toward advisory wording (`outperformer/top/winner`) trips the deterministic gate | compliance drift guard | Compliance review `reviews/b58-cohort-labels.md` cond-2 | OPEN (LOW) — governance hardening; not merge-blocking |
| B58-f2 | Hoist the per-category benchmark build OUT of the monthly-rescore per-portfolio loop (`tasks/mf.py` `_monthly_rescore` calls `_compute_cohort` per portfolio → re-fetches the same cohort dataset each time) | **before Plus go-live** (rescore at real Plus load) | Architect review `reviews/b58-cohort-labels.md` cond-1 | **RESOLVED 2026-06-12** (`feat/b58-perf-margin-b62-panel`) — `_compute_cohort` split into `_build_cohort_context` (ONE build per run over the union of Plus portfolios' holdings; B63 chunking preserved) + `_relative_from_context` (pure per-portfolio lookup); CAS path unchanged via the wrapper. Equivalence + single-build + Plus-only-union tests. Accepted residual: a portfolio turning Plus mid-run scores without category flags that month (conservative, self-heals next cycle). Tier-C panel ACCEPT-W-C, conds applied (`reviews/b58-f2-f4-b62-f2.md`). |
| B58-f3 | Add `ix_mf_funds_category` index + Alembic migration — the cohort peer-lookup `WHERE category IN (…)` is a seq scan on `mf_funds` | before the fund universe grows (ETF/debt/index) | Architect review `reviews/b58-cohort-labels.md` cond-2 | OPEN (LOW) — trivial on ~5k funds today |
| B58-f4 | Make `_MARGIN_PCT` category-class-aware — the flat 2.0pp band is effectively inactive for debt categories (sub-2pp dispersion) → near-universal `on_track` there | label quality for debt funds | Product review `reviews/b58-cohort-labels.md` cond-2 | **RESOLVED 2026-06-12** (`feat/b58-perf-margin-b62-panel`) — **model v1.1**: margin keyed on AMFI class prefix (Debt 0.5pp · Hybrid 1.0pp · default 2.0pp; unknown → wider default, on_track fail-safe); manifest `ranking_configs_v1.json` `labels.cohort_margin_pct` lockstep test-enforced; changelog v1.1 entry + ADR-0030; **registry activation row written at deploy** (two-person gate, founder = approved_by) — until that row exists the resolution is code-complete, not activated. Recalibration trigger: >30% single-cycle flip in any debt sub-category. Accepted dead-band: liquid/overnight (~0.1pp spreads) stay inside even 0.5pp. **v1.1 registry ACTIVATED 2026-06-12 evening** (row `a7aa1e86`, two_person_ok, at the `ad1657f` deploy — activation no longer pending). |
| B58-f5 | UI label-glossary note that `out_of_form` is currently unreachable (needs fundamentals) + feature-doc note that benchmark quality depends on AMFI category-taxonomy consistency | user trust / explainability | Product review `reviews/b58-cohort-labels.md` | OPEN (LOW) — copy/UI layer |
| B59 | **Portfolio Intelligence — overlap + concentration.** Two new endpoints (`GET /api/v1/portfolio/{id}/overlap` and `/concentration`) with factual observational analysis of user holdings. Merge-eligible, NOT deployed. No new migration; reads `mf.*` only. Follow-up f1: integrate overlap/concentration links into the dashboard summary card. Follow-up f2: add constituent-level stock overlap once `MfFundConstituent` data is ingested. | MF-first wedge UX; Plus feature context | `feat/portfolio-intelligence-overlap-concentration` commits `4541c86`/`02ad6d1` | **MERGE-ELIGIBLE** — Gates: 22 backend unit + ci_guards + ruff; 15 FE vitest + tsc + eslint. Compliance inline ACCEPT. Deploy = human-gated. |
| B60 | **Data Transparency & Explainability — Plan Group 9 / PU2.** `GET /api/v1/portfolio/{id}/transparency` (authed, IDOR-guarded) returns per-fund confidence band + educational drivers + data sources + NAV freshness + explicit `insufficient_data` refusal (PU2). Frontend: `TransparencyPanel` component. New `backend/dhanradar/transparency/` module; read-only over persisted `mf.*` tables; `unified_score` never selected or serialized; disclosure bundle on every response. Independent reviewer PASS (post B2 fix: "freshness check recommended" → "this label uses older price data"; advisory verb lists expanded to full SEBI set; late-import comment added). Deterministic gates: ruff + ci_guards + anti-pattern + tsc clean; vitest 14/14. Requires Opus compliance gate before merge (numeric-boundary + advisory-boundary surface). | MF-first differentiator; trust; PU2 | `feat/data-transparency-layer` commits `eab7db7`, `c136962`, `8e4ab7c`, `3a74d68`, `dde88cd` | **RESOLVED — merged #80 (`5cd1c48`) 2026-06-11; Opus compliance gate ACCEPT (PR #80 ledger).** Panel **mounted 2026-06-12** (`feat/b60-transparency-mount`): `TransparencySection` wraps `usePortfolioTransparency`→`TransparencyPanel`, last on `/portfolio/[id]/intelligence`; shell-always + h2 invariant; Architect + UI ACCEPT-WITH-CONDITIONS, both conditions applied (`queryKeys.portfolio.transparency` factory entry; shell heading 16px); ledger `reviews/b60-transparency-mount.md`. Deploy human-gated. Follow-up: (a) add empty-portfolio (zero scores) integration test; (b) B56-f1 move disclosure constants to shared module coordinates here too. |
| B61 | **Daily NAV refresh has never succeeded — duplicate `(isin, nav_date)` pairs abort the bulk upsert.** `nav_daily_fetch` fetches AMFI NAVAll.txt successfully (HTTP 200, ~14,208 rows) but the bulk upsert fails with `asyncpg.exceptions.CardinalityViolationError: ON CONFLICT DO UPDATE command cannot affect row a second time` → returns `"nav_daily_fetch: failed"`, 0 rows written. Root cause (data/ingestion): `_navrows_to_nav_upserts` keys on `isin_growth or isin_reinvest`, collapsing growth/reinvest plan variants of the same fund into the same ISIN. When both variants appear in one NAVAll.txt batch, the same `(isin, nav_date)` pair appears twice in the INSERT list; Postgres rejects a single statement that would update the same row twice. The same dedup gap affects `_navrows_to_fund_upserts` (keyed on `isin`). This is DISTINCT from the asyncpg InterfaceError fixed in PR #74 — NAV fetch now reaches AMFI, which it could not before. Found 2026-06-11 by manually triggering `nav_daily_fetch` post-deploy to verify the PR #74 fix. Impact: daily NAV will not auto-refresh; the prod 2,027,380-row backfill (2026-06-10) goes stale until this is fixed. CAS labels remain correct from the existing backfill but will drift as NAV ages. Proposed fix: deduplicate parsed rows in `backend/dhanradar/tasks/mf.py` — in `_navrows_to_nav_upserts` (~lines 81–107) keep last-seen row per `(isin, nav_date)`; in `_navrows_to_fund_upserts` (~lines 109–126) keep last-seen per `isin`. Add a unit test feeding duplicate `(isin, nav_date)` rows and asserting one deduped output row. Fixes both `nav_daily_fetch` and `nav_backfill` (shared helpers). Follow-up to PR #74. | Daily NAV currency; MF label freshness | `backend/dhanradar/tasks/mf.py` ~lines 81–126 (`_navrows_to_nav_upserts`, `_navrows_to_fund_upserts`) | **RESOLVED 2026-06-11 — PR #81 (`e8d8463`).** Dedup of `_navrows_to_nav_upserts` (by `(isin, nav_date)`) and `_navrows_to_fund_upserts` (by `isin`) in `backend/dhanradar/tasks/mf.py` fixed the `CardinalityViolationError`. Verified in prod: `nav_daily_fetch` now succeeds ("14,041 navs, 14,037 funds"), `mf_funds` went 0 → 14,037, NAV refreshes nightly. 3 dedup unit tests added. See RCA entry 2026-06-11 in `docs/rca/README.md`. |
| B62 | **"What Changed" Explainability Engine — Plan Group 2.** `GET /api/v1/portfolio/{id}/changes` diffs the two most-recent `mf_user_fund_score_history` snapshots per fund → `change_kind` (improved/weakened/unchanged/new/insufficient_data) + educational reasons + band transition + NAV freshness; honest new/insufficient_data framing (no fabricated diff). New isolated read-only `backend/dhanradar/changes/` module + `WhatChangedPanel` (not yet mounted on a page). No `unified_score`/numeric in DOM; disclosure on every response. | MF-first differentiator; Plan Group 2 | `backend/dhanradar/changes/`; `docs/features/what-changed.md` | **RESOLVED 2026-06-11 — merged #82 (`3eaac87`) + DEPLOYED (`e7e416e`).** Opus compliance/numeric-boundary gate ACCEPT (no numeric leak; reasons factual; "improved/weakened" = "stronger/weaker category-relative form" + amber-not-red chip; honest new/insufficient_data; disclosure rendered; auth 401 / IDOR 404; cold-start 200; lane-isolated). 23 unit + 11 integration + 19 vitest; CI green. Live: `/changes` → 401 anon. Follow-ups **B62-f1** (chip-tint cosmetic CSS `${color}22` on a `var()`) — **RESOLVED 2026-06-12** (`fix/b62-f1-chip-tint`): `color-mix(in srgb, …)` tint keeps the `var()` valid; source-guard vitest prevents recurrence; RCA 2026-06-12; **B62-f2** (mount the panel on a portfolio page) — **RESOLVED 2026-06-12** (`feat/b58-perf-margin-b62-panel`): `WhatChangedSection` wraps `usePortfolioChanges`→`WhatChangedPanel`, mounted first on `/portfolio/[id]/intelligence`; transient shell mirrors the panel's token surface + h2 (UI review conds applied); 8 vitest cases. |
| B63 | **Cohort benchmark build is memory-unbounded — loads peer NAV series into Python.** Once the NAV backfill became COMPLETE (5.94M rows, 2026-06-11 post-SEV1 rebuild; the prior 2.03M was a partial pre-B61 artifact), the CAS scoring step OOM-killed (SIGKILL) the 256M celery-batch worker building the per-category peer median (1Y/3Y return + drawdown) in-process. Interim: batch worker limit 256M→640M, funded by cuts at ≥2.5× live usage (redis 256→128, fastapi 512→448, nextjs 448→320, cloudflared 128→64) so the §A6 3072M budget HOLDS (`check_compose_memory.py` green). If 640M still OOMs, the answer is the proper fix, not more RAM. Proper fix: push the cohort aggregation into SQL (median/drawdown per category computed DB-side, return scalars only), then restore a tighter worker limit. Escalates B58-f2/f3 at real data scale; required before the fund universe or user base grows. | scoring at data scale; worker memory | `backend/dhanradar/mf/cohort.py`; `tasks/mf.py` `_compute_cohort`; RCA 2026-06-11 | **RESOLVED 2026-06-11 — PR #95 (`61a1a58`), deployed.** Peer NAV series now load in `_COHORT_PEER_CHUNK=200` batches — peak memory is one batch regardless of cohort size; per-peer stats and category medians unchanged (regression test asserts chunked == one-shot). Verified live: both sample CAS files score end-to-end at 640M (E2E 2026-06-11). The SQL-side aggregation remains an OPTIONAL perf optimization (fewer round-trips), no longer a correctness/memory need. |
| B64 | DPDP counsel confirmation: cross-border transactional auth email (Resend, non-Indian processor) for email OTP login — payload is email address + ephemeral 6-digit code; trigger is user-initiated (user clicks "Sign in with email code"); a pre-auth consent gate would create lockout circularity (must log in to grant consent, but needs consent to receive the login code). Distinct from B31 (system-initiated notification fan-out). Escalate to counsel; do not self-certify. Email template stays strictly transactional. `RESEND_API_KEY` provisioned + feature LIVE 2026-06-12 (deploy `ad1657f`); only the counsel confirmation remains open. Surface: email OTP go-live (DPDP). Ledger: `reviews/email-otp-login.md`. | email OTP go-live (DPDP) | `reviews/email-otp-login.md`; ADR-0031 | OPEN (LOW) |
| B65 | **`[RESOLVED — fix/b65-cams-xirr-sign]` 2026-06-12 — CAMS CAS XIRR null (sign-convention normalisation gap).** CAMS CAS report showed `xirr_pct: null` despite the statement carrying full transaction history (founder E2E: 11 funds, value ₹94,767, invested ₹91,812). Root cause: `parse_cas` did not normalise casparser's statement sign convention (purchases POSITIVE in CAMS output) to the investor convention (`ParsedTxn` contract + `snapshot.xirr()` require purchases NEGATIVE). All-purchase portfolios produced all-positive flows; the all-same-sign guard in `snapshot.xirr()` (`backend/dhanradar/mf/snapshot.py:76-82`) returned None. Contributing factors: test fixtures encoded the developer's assumed convention rather than real casparser output (injectable-reader design kept CI green), and no worker observability logged the XIRR outcome. Fix: per-type sign normalisation at the parse boundary in `backend/dhanradar/mf/cas.py` — DIVIDEND_PAYOUT kept as printed, non-cash flow types excluded, everything else negated; handles casparser 1.0 enum and 0.7.x string types. 7 regression tests added (including exact bug repro + type-fake that fails a `str()`-based implementation); fixtures migrated to realistic statement convention; observability added (`cas.parse` logs excluded txn counts; worker logs `mf.snapshot.built {funds, cashflows, xirr_computed}` boolean per build). 790 unit tests green; integration in CI. See RCA entry 2026-06-12 in `docs/rca/README.md`. Residual (LOW): no CI sentinel against upstream casparser sign-convention drift — a known-good PDF fixture or a version-pin guard would close it (architect finding #7). | MF XIRR correctness | `backend/dhanradar/mf/cas.py`; `backend/dhanradar/mf/snapshot.py:76-82` | RESOLVED — merged #109 (`412f126`) 2026-06-13; DEPLOYED 2026-06-13 via `scripts/deploy.sh` (smoke green; box at `412f126`; alembic `0018` unchanged; fix verified in the deployed celery-batch image). Ledger `reviews/b65-cams-xirr-sign.md`. Founder live-proof pending: re-upload the CAMS CAS PDF and confirm `xirr_pct` non-null (worker log `mf.snapshot.built xirr_computed=true` is the signal) |

## Notes

- **B6 is intentionally non-blocking for implementation.** Scoring-engine code and `ranking_configs`
  *staging* may proceed; the `approved_by ≠ created_by` methodology gate is enforced only before a
  version is **activated** in production. Track here until then.
- **B5 is RESOLVED** — `.github/workflows/ci.yml` enforces the deterministic plane on PR + push to
  `main` (`ci_guards.py` guards + secret scan, pytest, frontend build). **B12 ADDRESSED** (guard
  broadened + now scans token files); residual coverage gaps tracked as **B13**. Optional local
  `PreToolUse` commit hook still not wired.
- **B7/B8/B2 — DEFERRED, DATA-ONLY (no code action): real Razorpay seeding awaits the dashboard.**
  Re-verified in code 2026-06-07 (this slice): the fail-safes are present and correct, so charges
  cannot be created from wrong/absent config. What remains is **data-only seeding at launch**, gated
  on the Razorpay dashboard being provisioned — there is **no further implementation**.
  **FOUNDER DECISION RE-CONFIRMED 2026-06-11 (binding):** the documented PHASE 5M behavior stands —
  freemium + Founding Access (every signup stamped Plus-free until `FOUNDING_ACCESS_UNTIL`,
  placeholder 2026-12-31, reset to billing-go-live + 30d grace at go-live; founding ₹599/yr locked).
  **No fixed 90-day promo cap.** Razorpay is configured only when billing becomes applicable
  (registered business + KYC + dashboard). Until then checkout stays fail-closed (503/free) by
  design — do not seed, do not revisit.
  - **Verified fail-safes:** checkout REFUSES with **503** when a plan is unseeded —
    `if plan.razorpay_plan_id is None or plan.total_count is None` (`billing/service.py:113`); tier
    derivation is fail-safe — an `plan_id` not in `EXACT_PLAN_TIERS` grants **NO paid tier** (returns
    `free` + logs an error), never a substring/guess (`subscriptions/service.py:_derive_tier`, ~L52-73).
  - **What to seed (data only, when the Razorpay dashboard exists):** (1) `billing.plans.razorpay_plan_id`
    = the REAL dashboard plan id per plan (B7); (2) `billing.plans.total_count` = each plan's own cycle
    count (B8, e.g. 12 monthly / 1 annual — never the old hardcoded constant); (3) the
    `EXACT_PLAN_TIERS` map in `subscriptions/service.py` = exact `plan_id → tier` (B2). Plus the
    plans-seed copy Compliance pass (B9 residual). Until all three are seeded, billing stays
    fail-closed (503 / free), which is the intended pre-launch state.
- **B11** is an architecture-owner decision (a `FINAL_SCORING_SPEC` §2.5/§6.1 reconciliation), gated
  to scoring v1 activation alongside B6.
