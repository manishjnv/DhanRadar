# API Spec

REST, JSON, `/v1`, cursor pagination, RFC7807 errors, Bearer JWT. Full catalog: /docs/03-backend-architecture.md (Part E). Generate OpenAPI from FastAPI routers; generate TS types via openapi-typescript for the frontend. Premium fields gated server-side; public score ungated on stock/fund pages.
