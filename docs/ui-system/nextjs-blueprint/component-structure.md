# Component Structure

Three tiers: primitives (`components/ui`, cva variants, token-driven, a11y), composites/charts (`components/charts`,`feedback`), feature components (`features/*/components`). RSC by default; `"use client"` only for interactivity. Four data-states standard. See /components/*.md for per-component specs.
