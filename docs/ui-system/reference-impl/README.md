# Reference Implementation (starter code)

Real, compile-ready starters proving the token→component path. Copy into your Next.js app:
- `lib/cn.ts`, `lib/apiClient.ts`, `lib/queryKeys.ts`
- `tailwind.preset.ts` (import in tailwind.config.ts; pair with /tokens/css-variables.css → styles/tokens.css)
- `components/ui/Button.tsx`, `Card.tsx`; `components/charts/ScoreRing.tsx`
Build the rest from /components/*.md specs using these patterns (cva + tokens + Radix + four states).
