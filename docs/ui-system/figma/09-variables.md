# Figma Variables

Color collection (Light/Dark modes) + Primitives collection. The `/design-system/figma-variables.json` export was retired (design-system deleted 2026-06-06); canonical tokens now live in `frontend/` (`frontend/styles/tokens.json`). Bind components to variables; switching the page/frame mode previews dark instantly.
Groups: brand/*, accent/*, semantic/*, bg/*, surface/*, border/*, text/*, space/*, radius/*, bp/*.
